import { randomUUID } from "node:crypto";
import { Prisma } from "@prisma/client";

import { prisma } from "@/lib/db";
import type { CanvasDocument, CanvasElement } from "./design-canvas-state";
import { canvasElementKind, createCanvasDocument } from "./design-canvas-state";
import { DESIGN_ELEMENT_KINDS, assertFiniteDesignGeometry, type DesignDiscipline, type DesignElementKind } from "./design-studio";
import type { CommercialActor } from "./commercial-access";
import { commercialReadScope } from "./commercial-access";
import type { DesignCollaborationActor } from "./design-collaboration-policy";
import { requireDesignPermission } from "./design-collaboration-policy";

export type DesignProjectSummary = { id:string; organizationId:string; name:string; status:string; workingRevision:number; updatedAt:Date };
export type DesignFloorView = { id:string; organizationId:string; designProjectId:string; name:string; levelOrder:number; canvasWidth:Prisma.Decimal; canvasHeight:Prisma.Decimal; scaleUnit:string; realUnitsPerDesignUnit:Prisma.Decimal|null; updatedAt:Date };
type DesignElementRow = { id:string; designLayerId:string; kind:string; geometryJson:string; metadataJson:string|null };
type DesignRepositoryActor = CommercialActor & Pick<DesignCollaborationActor, "id" | "groups">;

function cleanName(value:string,label:string){const name=value.trim();if(!name)throw new Error(`${label} is required`);if(name.length>191)throw new Error(`${label} is too long`);return name;}
function parseDesignElementKind(value:string):DesignElementKind{if(!DESIGN_ELEMENT_KINDS.includes(value as DesignElementKind))throw new Error("Unsupported design element kind");return value as DesignElementKind;}
function collaborationActor(actor:DesignRepositoryActor):DesignCollaborationActor{return{id:actor.id,role:actor.role,organizationId:actor.organizationId,groups:actor.groups};}
function requireDesignRead(actor:DesignRepositoryActor,organizationId:string){return requireDesignPermission(collaborationActor(actor),organizationId,"VIEW");}
function requireDesignWrite(actor:DesignRepositoryActor,organizationId:string){return requireDesignPermission(collaborationActor(actor),organizationId,"EDIT");}
async function assertProjectForWrite(organizationId:string,projectId:string){const rows=await prisma.$queryRaw<Array<{id:string}>>(Prisma.sql`SELECT id FROM DesignProject WHERE id=${projectId} AND organizationId=${organizationId} LIMIT 1`);if(!rows[0])throw new Error("Design project not found");}
async function ensureDefaultLayer(tx:Prisma.TransactionClient,organizationId:string,projectId:string,floorId:string){const rows=await tx.$queryRaw<Array<{id:string}>>(Prisma.sql`SELECT id FROM DesignLayer WHERE organizationId=${organizationId} AND designProjectId=${projectId} AND designFloorId=${floorId} ORDER BY sortOrder ASC,createdAt ASC LIMIT 1`);if(rows[0])return rows[0].id;const id=randomUUID();await tx.$executeRaw(Prisma.sql`INSERT INTO DesignLayer (id,organizationId,designProjectId,designFloorId,discipline,name,sortOrder,isVisible,isLocked,createdAt,updatedAt) VALUES (${id},${organizationId},${projectId},${floorId},'CCTV','CCTV',0,TRUE,FALSE,NOW(3),NOW(3))`);return id;}
function parseElement(row:DesignElementRow):CanvasElement{const geometry=JSON.parse(row.geometryJson) as CanvasElement["geometry"];assertFiniteDesignGeometry(geometry);let metadata:{locked?:boolean;hidden?:boolean;discipline?:DesignDiscipline;category?:string;cameraFov?:CanvasElement["cameraFov"];cameraDori?:CanvasElement["cameraDori"];cameraSimulation?:CanvasElement["cameraSimulation"];cableRoute?:CanvasElement["cableRoute"];networkAddressing?:CanvasElement["networkAddressing"];topologyConnection?:CanvasElement["topologyConnection"]}={};if(row.metadataJson){try{metadata=JSON.parse(row.metadataJson) as typeof metadata;}catch{metadata={};}}return{id:row.id,kind:parseDesignElementKind(row.kind),geometry,layerId:row.designLayerId,discipline:metadata.discipline,category:metadata.category,cameraFov:metadata.cameraFov,cameraDori:metadata.cameraDori,cameraSimulation:metadata.cameraSimulation,cableRoute:metadata.cableRoute,networkAddressing:metadata.networkAddressing,topologyConnection:metadata.topologyConnection,locked:Boolean(metadata.locked),hidden:Boolean(metadata.hidden)};}

export async function listDesignProjects(actor:DesignRepositoryActor,requestedOrganizationId?:string){const scope=commercialReadScope(actor,requestedOrganizationId);requireDesignRead(actor,scope.organizationId);return prisma.$queryRaw<DesignProjectSummary[]>(Prisma.sql`SELECT id,organizationId,name,status,workingRevision,updatedAt FROM DesignProject WHERE organizationId=${scope.organizationId} ORDER BY updatedAt DESC`);}
export async function getDesignProject(actor:DesignRepositoryActor,projectId:string,requestedOrganizationId?:string){const scope=commercialReadScope(actor,requestedOrganizationId);requireDesignRead(actor,scope.organizationId);const rows=await prisma.$queryRaw<DesignProjectSummary[]>(Prisma.sql`SELECT id,organizationId,name,status,workingRevision,updatedAt FROM DesignProject WHERE id=${projectId} AND organizationId=${scope.organizationId} LIMIT 1`);return rows[0]??null;}
export async function listDesignFloors(actor:DesignRepositoryActor,projectId:string,requestedOrganizationId?:string){const scope=commercialReadScope(actor,requestedOrganizationId);requireDesignRead(actor,scope.organizationId);return prisma.$queryRaw<DesignFloorView[]>(Prisma.sql`SELECT f.id,f.organizationId,f.designProjectId,f.name,f.levelOrder,f.canvasWidth,f.canvasHeight,f.scaleUnit,f.realUnitsPerDesignUnit,f.updatedAt FROM DesignFloor f JOIN DesignProject p ON p.id=f.designProjectId AND p.organizationId=f.organizationId WHERE f.designProjectId=${projectId} AND f.organizationId=${scope.organizationId} ORDER BY f.levelOrder ASC,f.createdAt ASC`);}
export async function loadDesignFloorCanvas(actor:DesignRepositoryActor,projectId:string,floorId:string,requestedOrganizationId?:string):Promise<CanvasDocument>{const scope=commercialReadScope(actor,requestedOrganizationId);requireDesignRead(actor,scope.organizationId);const floorRows=await prisma.$queryRaw<Array<{id:string}>>(Prisma.sql`SELECT f.id FROM DesignFloor f JOIN DesignProject p ON p.id=f.designProjectId AND p.organizationId=f.organizationId WHERE f.id=${floorId} AND f.designProjectId=${projectId} AND f.organizationId=${scope.organizationId} LIMIT 1`);if(!floorRows[0])throw new Error("Design floor not found");const rows=await prisma.$queryRaw<DesignElementRow[]>(Prisma.sql`SELECT id,designLayerId,kind,geometryJson,metadataJson FROM DesignElement WHERE organizationId=${scope.organizationId} AND designProjectId=${projectId} AND designFloorId=${floorId} ORDER BY createdAt ASC`);return createCanvasDocument(rows.map(parseElement));}

export async function saveDesignFloorCanvas(actor:DesignRepositoryActor,input:{organizationId:string;projectId:string;floorId:string;expectedRevision:number;document:CanvasDocument}){const organizationId=requireDesignWrite(actor,input.organizationId.trim());if(!Number.isInteger(input.expectedRevision)||input.expectedRevision<1)throw new Error("Invalid working revision");if(input.document.schemaVersion!==1)throw new Error("Unsupported canvas schema version");for(const element of input.document.elements){assertFiniteDesignGeometry(element.geometry);parseDesignElementKind(canvasElementKind(element));}return prisma.$transaction(async tx=>{const projectRows=await tx.$queryRaw<Array<{workingRevision:number}>>(Prisma.sql`SELECT workingRevision FROM DesignProject WHERE id=${input.projectId} AND organizationId=${organizationId} FOR UPDATE`);const project=projectRows[0];if(!project)throw new Error("Design project not found");if(project.workingRevision!==input.expectedRevision)throw new Error(`Design changed elsewhere. Reload required (server revision ${project.workingRevision}).`);const floorRows=await tx.$queryRaw<Array<{id:string}>>(Prisma.sql`SELECT id FROM DesignFloor WHERE id=${input.floorId} AND designProjectId=${input.projectId} AND organizationId=${organizationId} LIMIT 1`);if(!floorRows[0])throw new Error("Design floor not found");const defaultLayerId=await ensureDefaultLayer(tx,organizationId,input.projectId,input.floorId);const availableLayers=await tx.$queryRaw<Array<{id:string}>>(Prisma.sql`SELECT id FROM DesignLayer WHERE organizationId=${organizationId} AND designProjectId=${input.projectId} AND designFloorId=${input.floorId}`);const availableLayerIds=new Set(availableLayers.map((layer)=>layer.id));await tx.$executeRaw(Prisma.sql`DELETE FROM DesignElement WHERE organizationId=${organizationId} AND designProjectId=${input.projectId} AND designFloorId=${input.floorId}`);for(const element of input.document.elements){const kind=canvasElementKind(element),geometryJson=JSON.stringify(element.geometry),layerId=element.layerId&&availableLayerIds.has(element.layerId)?element.layerId:defaultLayerId,metadataJson=JSON.stringify({locked:Boolean(element.locked),hidden:Boolean(element.hidden),discipline:element.discipline,category:element.category,cameraFov:element.cameraFov,cameraDori:element.cameraDori,cameraSimulation:element.cameraSimulation,cableRoute:element.cableRoute,networkAddressing:element.networkAddressing,topologyConnection:element.topologyConnection});await tx.$executeRaw(Prisma.sql`INSERT INTO DesignElement (id,organizationId,designProjectId,designFloorId,designLayerId,kind,geometryJson,metadataJson,schemaVersion,createdAt,updatedAt) VALUES (${element.id},${organizationId},${input.projectId},${input.floorId},${layerId},${kind},${geometryJson},${metadataJson},1,NOW(3),NOW(3))`);}const nextRevision=input.expectedRevision+1;const changed=await tx.$executeRaw(Prisma.sql`UPDATE DesignProject SET workingRevision=${nextRevision},updatedAt=NOW(3) WHERE id=${input.projectId} AND organizationId=${organizationId} AND workingRevision=${input.expectedRevision}`);if(!changed)throw new Error("Design revision conflict");await tx.$executeRaw(Prisma.sql`UPDATE DesignFloor SET updatedAt=NOW(3) WHERE id=${input.floorId} AND designProjectId=${input.projectId} AND organizationId=${organizationId}`);return nextRevision;});}
export async function createDesignProject(actor:DesignRepositoryActor,input:{organizationId:string;name:string;createdByUserId:string}){const organizationId=requireDesignWrite(actor,input.organizationId.trim());const id=randomUUID();await prisma.$executeRaw(Prisma.sql`INSERT INTO DesignProject (id,organizationId,name,status,createdByUserId,workingRevision,createdAt,updatedAt) VALUES (${id},${organizationId},${cleanName(input.name,"Design project name")},'DRAFT',${input.createdByUserId},1,NOW(3),NOW(3))`);return id;}
export async function createDesignFloor(actor:DesignRepositoryActor,input:{organizationId:string;projectId:string;name:string;levelOrder?:number}){const organizationId=requireDesignWrite(actor,input.organizationId.trim());await assertProjectForWrite(organizationId,input.projectId);const levelOrder=input.levelOrder??0;if(!Number.isInteger(levelOrder))throw new Error("Floor order must be an integer");const id=randomUUID();await prisma.$executeRaw(Prisma.sql`INSERT INTO DesignFloor (id,organizationId,designProjectId,name,levelOrder,canvasWidth,canvasHeight,scaleUnit,createdAt,updatedAt) VALUES (${id},${organizationId},${input.projectId},${cleanName(input.name,"Floor name")},${levelOrder},1000,1000,'FT',NOW(3),NOW(3))`);await prisma.$executeRaw(Prisma.sql`UPDATE DesignProject SET workingRevision=workingRevision+1,updatedAt=NOW(3) WHERE id=${input.projectId} AND organizationId=${organizationId}`);return id;}
export async function renameDesignFloor(actor:DesignRepositoryActor,input:{organizationId:string;projectId:string;floorId:string;name:string}){const organizationId=requireDesignWrite(actor,input.organizationId.trim());await assertProjectForWrite(organizationId,input.projectId);const changed=await prisma.$executeRaw(Prisma.sql`UPDATE DesignFloor SET name=${cleanName(input.name,"Design floor name")},updatedAt=NOW(3) WHERE id=${input.floorId} AND designProjectId=${input.projectId} AND organizationId=${organizationId}`);if(!changed)throw new Error("Design floor not found");}
